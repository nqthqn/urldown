{
  "name": "urldown",
  "version": "0.0.0",
  "description": "Takes email addresses and send out unique URLs for downloading files. The URLs expire after a set number of usages.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nate Nichols",
  "license": "BSD-2-Clause",
  "dependencies": {
    "hapi": "~5.0.0",
    "redis": "~0.10.2",
    "simple-ansi": "0.0.1",
    "handlebars": "~2.0.0-alpha.2",
    "poet": "~1.0.2"
  },
  "devDependencies": {
    "grunt": "~0.4.5"
  }
}
